// IIFE -> Stands for Immediately Invoke Function Expression

// função que é invocada quando é definida
// isso ajuda a fugir do escopo global já que o que foi definido na função é escopo local

(function() {
    console.log('Olá')
})()